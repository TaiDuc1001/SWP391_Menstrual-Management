// Appointments page for authenticated users
import React from 'react';

const Appointments: React.FC = () => (
  <div>
    <h1>Appointments</h1>
    <p>Manage your appointments here.</p>
  </div>
);

export default Appointments;
